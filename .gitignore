# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/

# Virtual environment
.venv/

# Test / lint / type-check caches and artifacts
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
.coverage.*
htmlcov/

# Build outputs (ts-sentry build-dataset --out; default is `build/`, but
# manual verification runs commonly use build_a/, build_b/, etc.)
/build/
/build_*/

# Saif's own phase-close verification artifacts. CLAUDE.md: these paths are his
# and are never to be deleted, overwritten or reused. They are also not release
# artifacts, and a `git add -A` during STEP-08 swept several of them into a
# release commit before this rule existed. Root-anchored so the curated
# `examples/` tree is unaffected.
/session_*/
/report_*/
